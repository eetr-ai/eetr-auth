---
description: Core architecture and layer conventions for progression-ai-auth
alwaysApply: true
---

# Architecture

- **Server actions**: No business logic. Only call `onServerAction` and then one or more service methods. See `src/app/actions/user-actions.ts`.
- **API route handlers**: No business logic. Use `withApiContext` and delegate to services. See `src/app/api/health/route.ts`.
- **Services**: All business logic lives in `src/lib/services/`. Services depend on `RequestContext` and repositories; they do not call `getCloudflareContext()` or use raw D1.
- **Persistence**: Only in `src/lib/repositories/` (D1). Repositories receive the DB (from context) in the constructor; no Cloudflare env in repository methods.
- **Frontend state management**: Default to reducer-driven state with `@eetr/react-reducer-utils` (action types, state, reducer, `bootstrapProvider`, Provider + hook) across pages and complex components. Use `useState` only for simple local state in simple components. No ad-hoc global state outside this pattern. See `src/context/admin-state.ts`.
- **Icons**: Use `lucide-react` only; import named icons (e.g. `import { IconName } from "lucide-react"`); do not add other icon libraries.
- **Where to add code**: New features add services/repos; entry points (actions, API routes) only wire context and call services; new client state domains add reducer modules under `src/context/` or `src/store/`.
- **Password hashing**: Set **`HASH_METHOD`** to `argon` (production) or `md5` (local/legacy). **`argon`** requires **`ARGON_HASHER`** bound; Argon2 via `POST /hash` and `POST /verify`; legacy **MD5 hex** (32-char) verifies locally then upgrades to Argon2. **`md5`** uses MD5 hex only and rejects Argon2-stored rows. Without `HASH_METHOD`, behavior defaults to `md5`. No PBKDF2. Configure `wrangler.jsonc` with `service: "argon-hasher"` and matching `props` (e.g. `internal: true`). Do not add native Node addons in the app bundle.
- **DB schema rule**: `db/schema.sql` is the full latest schema snapshot for fresh installs. `db/patches/<version>.sql` contains only the delta needed to move from the previous released schema to that version. Do not duplicate the full snapshot into patch files; when in doubt, update `schema.sql` to match the current real schema and keep patches upgrade-focused.
- **MFA OTP**: **`MFA_OTP_MAX_ATTEMPTS`** (default 5) — after that many wrong codes, the challenge row is deleted.
- **Secrets at rest (OAuth client_secret, future)**: Shared **HMAC-SHA256** helper + **`HMAC_KEY`** (Wrangler secret); stored values use a version prefix (e.g. `h1:`). Plaintext-at-rest legacy rows may still verify until upgraded.
